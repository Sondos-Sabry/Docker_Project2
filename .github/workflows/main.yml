on:
  push:
    branches:
      - master

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3

      -name: "Login to docker"
        run: docker login --username sondoshammad --password dckr_pat_UPJoIJvKMAuQ0tQjfqhpqGGqyCc
        
       